<%= form_for(@battle_participation) do |f| %>
  <% if @battle_participation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@battle_participation.errors.count, "error") %> prohibited this battle_participation from being saved:</h2>

      <ul>
      <% @battle_participation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :character_id %><br>
    <%= f.number_field :character_id %>
  </div>
  <div class="field">
    <%= f.label :battle_id %><br>
    <%= f.number_field :battle_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
