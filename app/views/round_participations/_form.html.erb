<%= form_for(@round_participation) do |f| %>
  <% if @round_participation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@round_participation.errors.count, "error") %> prohibited this round_participation from being saved:</h2>

      <ul>
      <% @round_participation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :round_id %><br>
    <%= f.number_field :round_id %>
  </div>
  <div class="field">
    <%= f.label :character_id %><br>
    <%= f.number_field :character_id %>
  </div>
  <div class="field">
    <%= f.label :point %><br>
    <%= f.number_field :point %>
  </div>
  <div class="field">
    <%= f.label :late_deduction %><br>
    <%= f.number_field :late_deduction %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
